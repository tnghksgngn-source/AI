cnn이란 합성곱 신경망을 뜻한다. 딥러닝에서 주로 이미지나 영상 데이터를 처리할 때 사용된다. dnn에 2차원 이미지를 1차원으로 평탄화 시켜서 입력하게 된다면 공간적/지역적 정보가 손실된다. 
이러한 문제를 해결한 것이 cnn이다. Cnn은 이미지를 날 것 그대로 받음으로써 공간적/지역적 정보를 유지한채 특성들의 계층을 빌드한다. 이때 사용하는 것이 합성곱 연산이다. 합성곱은 원본이미지에 지정된  크기의 필터를 슬라이딩하여 곱하고 더해서  이미지의 새로운 특징을 추출한다. 필터의 크기에 따라 추출되는 특징은 달라진다. 이미지의 크기를 계속 유지한채 분류부로 가게된다면 연산량이 급증한다. 따라서 플링을 통해 정보의 크기를 줄인다. 플링은 맥스 플링과 평균 플링이 있다. 맥스 플링은 선택영역에서 가장 큰 값을 선택하는 것과 같다. 맥스 플링을 사용하면 선택영역의 내부에서 픽셀이 이동하거나  회전하더라도 가장 수치가 높은 값을 선택하기 때문에 출력값은 동일하다. 스트라이드는 이미지에 대한 필터를 적용할 때 필터의 이동량이고, 패딩은 입력이미지에 대한 합성곱을 수행하게되면 출력이미지의 크기가 작아지는 문제를 해결한다. 입력이미지의 가장자리에 특정 값으로 설정된 픽셀을 추가함으로써 입력이미지와 출력이미지의 크기를 같게 만들어서 가장자리의 정보가 소실되는 것을 막는다

기존의 신경망(Dense, CNN)은 한 번에 입력된 데이터들을 각각 독립적인 것으로 취급하기 때문에, 문장, 음성, 주가 데이터처럼 '시간의 흐름'이나 '순서'가 의미를 가지는 시퀀스(Sequence) 데이터의 맥락을 파악하는 데 한계가 있다. 이런 단점을 보완하고 과거의 문맥이나 정보를 현재의 판단에 활용하기 위해 고안된 딥러닝 모델이 rnn이다. 
RNN의 가장 큰 특징은 데이터가 앞을 향해 한 방향으로만 흐르는 것이 아니라, 은닉층(Hidden Layer)의 결과값이 다음 시점(Time Step)의 연산을 위해 다시 자신에게 입력으로 들어오는 '순환(Recurrence)' 고리를 갖는다는 것이다. 즉, 현재 시점의 결과물은 '현재 들어온 입력값'과 '과거의 기억(이전 시점의 은닉 상태)'을 모두 조합하여 만들어진다.
Rnn은 역전파과정에서 가장 처음에 입력되었던 과거의 핵심정보가 희미해지는 장기의존성 문제가 발생한다. 이를 해결한 것이 과거 정보중에서 어던 정보를 잊고 어떤 정보를 끝까지 기억할지 결정하는 게이트를 추가한 것이 lstm이다. 게이트는 정보의 통과량을 0에서 1사이의 비율로 조절하는 시그모이드 함수를 사용하며 역할에 따라 망각,입력,출력 게이트가 있다. 망각 게이트는 과거의 정보와 현재 들어온 새로운 입력값을 살펴보고,과거의 기억 중 현재 시점에서 더 이상 필요 없는 불필요한 정보를 얼마나 지워버릴지 결정한다. 입력게이트는 현재 들어온 새로운 입력값 중에서, 어떤 유의미한 정보를 골라내어 새롭게 장기 기억에 저장할지 결정한다. 이렇게 망각게이트를 통해 과거 데이터를 적절히 지우고 입력게이트를 통해  중요한 새 정보를 더해서 새로운 셀 상태가 완성된다.
출력게이트를 바탕으로 다음 시점으로 넘겨주거나 현재 시점의 결과물로 내보낼 새로운 은닉 상태를 얼마나 출력할 지 결정한다. 
